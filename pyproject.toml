[tool.poetry]
name = "gcp_fipe"
version = "0.1.0"
description = "Pipeline de Engenharia de Dados para extração de dados da API FIPE"
authors = ["Danilo Natan"]

[tool.poetry.dependencies]
python = "^3.12"
requests = "^2.31.0"
python-dotenv = "^1.0.1"
pandas = "^2.0.0"
pandas-gbq = "^0.22.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
